# SHIELD Customer UI Audit

Last audited: 2026-08-08. The customer portal is protected by the customer JWT/session and rendered through `/portal/customer/:section`.

Route-level and membership/card screen manifests are maintained in `CUSTOMER_UI_ROUTE_INVENTORY.md` and `CUSTOMER_UI_SCREEN_MANIFEST.md`.
`CUSTOMER_UI_API_BINDINGS.md` and `CUSTOMER_UI_STATE_MATRIX.md` record the customer-safe API ownership and screen-state rules.
`CUSTOMER_UI_DESIGN_TOKENS.md`, `CUSTOMER_UI_COMPONENT_GUIDE.md`, and `CUSTOMER_UI_ACCESSIBILITY_CHECKLIST.md` record the shared customer visual and accessibility baseline.

| Route/section | Current screen | Binding | Redesign/gap |
|---|---|---|---|
| `/customer/login`, `/otp`, `/register` | Customer auth screens | Firebase phone OTP + customer auth repository | Visual alignment and auth-state QA remain |
| `dashboard` | `CustomerDashboardScreen` | `DashboardRepository`, cache, `GET /customer/dashboard` | Shared header displays compact API-derived cash and reward totals; Operations carousel precedes membership content; duplicate financial tiles were replaced by visits, documents, and unread updates; screenshot QA pending |
| `membership`, `membership/subscription`, `membership/benefits` | `CustomerMembershipScreen` focused views | `MembershipRepository`, `GET /customer/membership` | COMPLETE — supported overview, entitlement display, active/pending/expired/suspended states, cached offline state, refresh/recovery and 350px coverage. Structured coverage remains explicitly unavailable. Authenticated screenshots are deferred because Chrome control timed out and Playwright CLI crashed with a native Windows assertion; no evidence was fabricated. |
| `privilege-card` | `CustomerPrivilegeCardScreen` | `MembershipRepository`, customer-self card/status/history APIs, authenticated customer profile | COMPLETE — digital card plus supported physical-card request/status/history. Secure QR is deferred pending a security contract; lost and damaged cards are not supported, and replacement is deferred pending product and backend contracts. |
| `wallet` | `CustomerWalletScreen` | `WalletRepository`, `GET /customer/wallet` plus authenticated customer profile | Cash wallet and scoped transaction history exist. The customer access gate now uses the membership returned by the wallet contract, so active issued members reach the CASH ledger while `SHIELD_BENEFIT` remains filtered. A wallet API failure is a retryable unavailable state, never a displayed zero balance. Add-funds, exportable statements and transaction-detail routes lack verified customer contracts. |
| `wallet-history` | `CustomerWalletScreen(showFullHistory: true)` | `WalletRepository`, `GET /customer/wallet` | Full customer-scoped transaction list; reachable from Wallet when additional visible records exist; statement export remains unsupported |
| `rewards` | `CustomerRewardPointsScreen` | `WalletRepository`, `GET /customer/wallet`, `REWARD_POINTS` ledger only | Live balance and activity are implemented. Normalized `EARNED` reward entries render as credits; redemption/expiry require dedicated backend contracts. |
| `services` | `CustomerServicesScreen` | `CustomerProviderRepository`; `GET /customer/providers/categories`; `GET /customer/providers`; customer-safe detail projection | Extracted provider discovery with route-backed `type`, `q`, `page`, and `provider` detail state. Wellness products remain separate; booking and prescriptions retain existing routes. Location, coverage, popularity, favourites, hours, ratings, languages, practitioner profiles, and insurance directory are **NOT SUPPORTED IN CURRENT CONTRACT**. |
| `book-appointment` | Inline `_CustomerServicesView` consultation tab | Same supported provider and appointment APIs as `services` | Legacy route now opens the genuine booking flow instead of the generic portal renderer |
| `appointments` | `CustomerVisitsScreen` | Customer-scoped appointment list/cancel/reschedule APIs plus authenticated membership access check | Extracted Visits feature provides backend-status filters, safe summary details, loading/empty/retry, cancellation confirmation, and reschedule request. Slots, pricing, coverage, timeline, dependent patients, and online links lack customer contracts. |
| `documents` | `CustomerDocumentsScreen` | `GET /documents`, `POST /documents/upload`, `GET /documents/:id`, `GET /documents/:id/download` | Extracted repository/controller feature. Customer-owned archive has category filter, search, loading, empty, retry, allowlisted upload, safe details, and signed R2 view/download. Customer metadata excludes storage keys, staff/audit/extraction fields, and other-customer identifiers. Secure sharing and local-development streaming remain deferred. |
| `prescriptions` | `CustomerPrescriptionsScreen` | Customer documents plus `POST/GET /pharmacy/prescriptions` | Extracted customer prescription archive with truthful document status, upload, signed R2 view/download, preferred Pharmacy fallback, Services Pharmacy context preservation, explicit review/consent, and duplicate-safe request submission. OCR/clinical interpretation, fulfilment, payment, and delivery are not exposed. |
| `orders` | `CustomerOrdersScreen` / `CustomerOrdersRepository` | `GET /customer/orders`; `GET /customer/orders/:id` | Customer-owned recorded-purchase history and safe item-detail sheet have loading, empty, refresh, and retryable failure states; cart, checkout, tracking, return and refund APIs remain unavailable |
| `referrals` | `CustomerReferralsScreen` | `GET /referrals/me` | Session-owned safe referral code/status history with copy action; share links, QR, creation, and internal commission data remain unavailable |
| `activity` | `CustomerActivityScreen` | `GET /timeline/me` | Database-backed self timeline for membership, care, documents, wallet, and notifications; data-derived category filters; the endpoint takes no customer ID |
| `notifications` | Inline `_CustomerNotificationsView` | `GET /notifications/me`, `POST /notifications/:id/read`, `POST /notifications/mark-all-read` | Safe session-owned inbox and authoritative unread count; filter/read states remain retryable, while deep links/pagination require contracts |
| `profile`, `settings`, `account` | Inline portal views plus customer account workspace | Customer profile/membership/session APIs; customer account APIs; persisted support-contact and feedback APIs | Profile includes initials identity, verified-primary-mobile context, independently loaded membership number/status, persisted edits with validation, save feedback, discard protection, and direct paths to the routed Profile & Family workspace. The workspace persists addresses, dependents, typed alternative/emergency contacts, and preferred-pharmacy selection with loading, empty, retry, save, edit, and confirmed-remove states. Settings persists Push and SMS preferences, and continues to expose owner-scoped sessions, support, FAQ, policy, feedback, About, and confirmed sign-out. Primary-mobile change remains unavailable: a verified Firebase identity migration, duplicate-account check, session policy, and audit contract are not yet implemented end-to-end. |

## Shared foundation

`CustomerScaffold`, `CustomerAppBar`, `CustomerBottomNavigation`, shared error/empty/loading widgets, and `CustomerDesignTokens` provide the active customer shell. Profile-gated routes show a retryable access-status failure when the authenticated profile or membership contract cannot be verified; they never fall back to shared or dummy customer data. Header balances are derived from the existing dashboard bundle; SHIELD benefit is never rendered as cash. At constrained widths, balance actions collapse to icons rather than overflowing the app bar.

## Known blockers

- No verified customer API contract yet for full rewards, customer wellness cart/checkout, referral/activity, card lifecycle, or family/dependents.
- Operations carousel is database-backed through `commercial_settings` (`OPERATIONS_CUSTOMER_BANNERS`) and correctly renders an empty state when Operations has published no eligible banners. An idempotent non-production banner seed and local demo photos are available without overwriting an Operations-managed setting.
- The current customer services, visits, profile, notifications, and settings implementations are embedded in the large portal shell and must be extracted incrementally to preserve behavior.
- The unsupported customer recharge section was removed from the customer portal registry. The wallet has no verified customer top-up/request contract, so it must not claim a submitted recharge request.
