Complete the entire SHIELD customer-facing Flutter UI/UX inside `E:\K4NN4N\shield` using `E:\K4NN4N\shield\Design reference` as the only visual authority and the current repository/schema/APIs as the functional authority.

Before editing, read every file in the supplied `SHIELD_Customer_UI_Completion_Goal_Package`, beginning with `00_READ_FIRST.md` and `01_GOAL_OBJECTIVE.md`. Use `03_CUSTOMER_SCREEN_COMPLETION_MATRIX.csv` as the live completion tracker. Preserve current work, including the compact header, API-derived Cash Wallet and Reward Points chips, Operations carousel immediately below the header, corrected Dashboard hierarchy, Reward Points route, empty states and demo-catalogue disclosure.

Do not create a parallel app, use screenshots as backgrounds, hardcode customer data, merge SHIELD Benefit into cash, remove existing features, or return status-only messages. Audit the current state, then implement coherent vertical slices until every applicable matrix row has functional behavior, real API/database binding, the approved design language, loading/empty/error/offline states, responsive behavior, tests and screenshot QA. The Operations carousel is mandatory on Home even when absent from the closest reference image.
