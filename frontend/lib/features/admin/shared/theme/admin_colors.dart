import 'package:flutter/material.dart';

import '../../../../app/theme/app_colors.dart';

class AdminColors {
  static const primary = AppColors.shieldNavy;
  static const secondary = AppColors.shieldBlue;
  static const success = AppColors.success;
  static const warning = AppColors.warning;
  static const danger = AppColors.error;
  static const info = AppColors.info;
  static const rewards = Color(0xFF7C3AED);
  static const visits = Color(0xFF0EA5A8);
  static const surface = AppColors.white;
  static const mutedSurface = AppColors.lightGray;
  static const border = AppColors.divider;
  static const text = AppColors.shieldNavy;
  static const subtext = AppColors.darkGray;
  static const caption = AppColors.gray;
}
